[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B"]
ignore = [
    "E501",  # line too long — seed files have unavoidable long strings
    "B008",  # Depends() in default arg — standard FastAPI idiom
]
