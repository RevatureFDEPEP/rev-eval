[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
]
ignore = [
    "E501", # line too long — handled by formatter, not enforced here
]

[tool.ruff.lint.isort]
known-first-party = ["config", "db", "models", "repositories", "schemas", "services", "utils", "v1"]
