[pytest]
# Run pytest from this service dir. `pythonpath = .` puts the service root on
# sys.path so tests can `import main` and `from src...` the same way the app does.
pythonpath = .
testpaths = tests
addopts = -q
