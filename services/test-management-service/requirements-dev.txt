# Dev/test dependencies. Feeds both the Dockerfile `test` stage and local dev,
# and replaces the inline `pip install pytest pytest-cov ruff` step in CI.
-r requirements.txt
pytest>=8,<9
pytest-cov>=5,<7
ruff>=0.6,<1
# Async repo tests run against a hermetic in-memory DB (no Postgres needed).
pytest-asyncio>=0.23,<0.25
aiosqlite>=0.19,<0.21
# Integration suite (tests/integration/, gated behind --integration) seeds the
# question bank directly in Mongo before exercising the real cross-service call.
pymongo>=4.6,<5
