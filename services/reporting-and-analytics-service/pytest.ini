[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    integration: cross-schema tests against a real Postgres migrated by test-management-service; skipped unless --integration is passed
